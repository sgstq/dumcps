This is a collection of mcps.
The MCPs will be written in different programming languages and will have different features, so each MCP will be in its own subdirectory and inependent from the others. Each will have its own README.md, internal structure, and virtual environment if needed.
